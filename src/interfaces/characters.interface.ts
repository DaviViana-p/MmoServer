export interface characters {
    id: number;  
    nome: string;     
    characterinfo: string; 
    accountID: number; 
    map_id: number;  
    transform?: [number, number, number, number, number, number]; 
}